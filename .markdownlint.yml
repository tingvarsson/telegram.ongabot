# Duplicate headings (e.g. ### Added, ### Fixed) are expected across changelog version sections
MD024:
  siblings_only: true

# Line length not enforced — URLs and changelog entries regularly exceed 80 chars
MD013: false
